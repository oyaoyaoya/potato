<%= form_for(admin_textbook) do |f| %>
  <% if admin_textbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_textbook.errors.count, "error") %> prohibited this admin_textbook from being saved:</h2>

      <ul>
      <% admin_textbook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
