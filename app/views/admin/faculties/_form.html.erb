<%= form_for(admin_faculty) do |f| %>
  <% if admin_faculty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_faculty.errors.count, "error") %> prohibited this admin_faculty from being saved:</h2>

      <ul>
      <% admin_faculty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
