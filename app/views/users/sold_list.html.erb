<div class="row">
  <div class="col s12 m12 l6">
    <h5>出品リスト</h5>
    <% @items.each do |item| %>
    <div class="collection">
      <%= list_link(item) %>
        <%= item.name %>
        <%= status_tag(item) %>
      </a>
    </div>
    <% end %>
  </div>
</div>
